package base.strategy;

/**
 * The evaluation strategy when running on the application
 */
public class ApplicationEvaluationStrategy<X, Y> extends EvaluationStrategy<X, Y> {

    @Override
    protected void incorrectResult(X input, Y actualResult, Y expectedResult) {

    }
}
